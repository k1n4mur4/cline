# 学習モード（Learning Mode）実装ドキュメント

## 概要

Cline VSCode拡張機能に「学習モード」を追加。プロジェクトのオンボーディングを支援するための機能群を実装した。

**目的:**
- 新しいチームメンバーのプロジェクト理解を促進
- AIによるパーソナライズされた学習カリキュラムの生成
- 学習進捗の可視化と管理

---

## 実装フェーズ

| Phase | 名称 | 状態 | 主要機能 |
|-------|------|------|----------|
| Phase 1 | プロファイル設定 | ✅ 完了 | ユーザープロファイル管理、技術スタック検出 |
| Phase 1.5 | UI統合 | ✅ 完了 | サイドバーボタン、コマンドパレット |
| Phase 2 | カリキュラム生成 | ✅ 完了 | AI生成、プロジェクト分析、進捗管理 |
| Phase 3 | タスク実行支援 | ✅ 完了 | 詳細ビュー、統計、エクスポート、解説連携 |

---

## 全体アーキテクチャ

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              VSCode Extension                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐     │
│  │  Sidebar Button │    │ Command Palette │    │ Keyboard Shortcut│     │
│  │     🎓          │    │  Learning Mode  │    │  Cmd+Shift+L    │     │
│  └────────┬────────┘    └────────┬────────┘    └────────┬────────┘     │
│           │                      │                      │               │
│           └──────────────────────┼──────────────────────┘               │
│                                  ▼                                      │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                         Webview (React)                           │  │
│  ├──────────────────────────────────────────────────────────────────┤  │
│  │  App.tsx                                                          │  │
│  │  └─ LearningView                                                  │  │
│  │      ├─ ProfileSetupView  ← プロファイル未設定時                  │  │
│  │      └─ CurriculumView    ← プロファイル設定済み時                │  │
│  │          ├─ カリキュラム表示                                      │  │
│  │          ├─ TaskDetailPanel                                       │  │
│  │          ├─ StatisticsPanel                                       │  │
│  │          └─ エクスポートメニュー                                  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                  │                                      │
│                                  │ gRPC (protobus)                      │
│                                  ▼                                      │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                      Backend (TypeScript)                         │  │
│  ├──────────────────────────────────────────────────────────────────┤  │
│  │  LearningService                                                  │  │
│  │  ├─ saveProfile / getProfile                                      │  │
│  │  ├─ initEnvironment / detectTechStack                            │  │
│  │  ├─ getCurriculum / generateCurriculum / updateTaskProgress      │  │
│  │  ├─ getLearningStatistics                                        │  │
│  │  └─ exportCurriculum                                             │  │
│  ├──────────────────────────────────────────────────────────────────┤  │
│  │  Core Learning Components                                         │  │
│  │  ├─ UserProfileManager        - プロファイル永続化               │  │
│  │  ├─ OnboardingEnvironmentManager - .onboarding/管理              │  │
│  │  ├─ TechStackDetector         - 技術スタック自動検出             │  │
│  │  ├─ ProjectAnalyzer           - プロジェクト構造分析             │  │
│  │  ├─ CurriculumManager         - カリキュラム永続化               │  │
│  │  ├─ CurriculumGenerator       - AI生成ロジック                   │  │
│  │  ├─ LearningStatisticsManager - 学習統計管理                     │  │
│  │  └─ CurriculumExporter        - エクスポートロジック             │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                  │                                      │
│                                  ▼                                      │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                    File System (.onboarding/)                     │  │
│  │  ├─ profile.json      - ユーザープロファイル                      │  │
│  │  ├─ curriculum.json   - カリキュラムデータ                        │  │
│  │  └─ statistics.json   - 学習統計                                  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                          AI API (Anthropic)                              │
│  カリキュラム生成・コード解説に使用                                      │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 機能一覧

### Phase 1: プロファイル設定

| 機能 | 説明 |
|------|------|
| ユーザープロファイル管理 | 経験年数、役割、習熟度を保存 |
| 技術スタック自動検出 | package.json等からプロジェクトの技術を検出 |
| オンボーディング環境初期化 | .onboarding/ディレクトリの作成・管理 |

### Phase 1.5: UI統合

| 機能 | 説明 |
|------|------|
| サイドバーボタン | 🎓アイコンで学習モード起動 |
| コマンドパレット | "Learning Mode"コマンドで起動 |
| キーボードショートカット | Cmd/Ctrl+Shift+Lで起動 |

### Phase 2: カリキュラム生成

| 機能 | 説明 |
|------|------|
| プロジェクト分析 | ディレクトリ構造、アーキテクチャパターンの検出 |
| AIカリキュラム生成 | ユーザープロファイルに基づく動的生成 |
| タスク進捗管理 | チェックボックスでステータス変更 |
| ストリーミング進捗表示 | 生成中のリアルタイム進捗表示 |

### Phase 3: タスク実行支援

| 機能 | 説明 |
|------|------|
| タスク詳細ビュー | 説明、目安時間、関連ファイル表示 |
| ファイルジャンプ | 関連ファイルをVSCodeで開く |
| 学習統計 | 進捗グラフ、学習時間、連続日数 |
| エクスポート | Markdown/JSON形式でダウンロード |
| コード解説連携 | Clineチャットに解説を依頼 |

---

## ファイル構成

### Proto定義

```
proto/cline/learning.proto
```

**メッセージ:**
- UserProfile
- Curriculum, Chapter, LearningTask
- CurriculumGenerationProgress
- LearningStatistics, ChapterProgress, TaskStatistic
- 各種Request/Response

**サービス:**
- LearningService（10 RPCs）

### バックエンド

```
src/core/learning/
├── index.ts                    # エクスポート
├── types.ts                    # 型定義
├── UserProfileManager.ts       # プロファイル管理
├── OnboardingEnvironmentManager.ts  # 環境管理
├── TechStackDetector.ts        # 技術検出
├── ProjectAnalyzer.ts          # プロジェクト分析
├── CurriculumManager.ts        # カリキュラム管理
├── CurriculumGenerator.ts      # AI生成
├── LearningStatisticsManager.ts # 統計管理
└── CurriculumExporter.ts       # エクスポート

src/core/controller/learning/
├── saveProfile.ts
├── getProfile.ts
├── initEnvironment.ts
├── detectTechStack.ts
├── getCurriculum.ts
├── generateCurriculum.ts       # ストリーミング
├── updateTaskProgress.ts
├── getLearningStatistics.ts
└── exportCurriculum.ts
```

### フロントエンド

```
webview-ui/src/components/learning/
├── LearningView.tsx            # 統合ビュー
├── ProfileSetupView.tsx        # プロファイル設定
├── CurriculumView.tsx          # カリキュラム表示（詳細パネル含む）
└── StatisticsPanel.tsx         # 統計表示
```

### データストレージ

```
<workspace>/.onboarding/
├── profile.json                # ユーザープロファイル
├── curriculum.json             # カリキュラムデータ
└── statistics.json             # 学習統計
```

---

## 使用方法

### 学習モードの起動

| 方法 | 操作 |
|------|------|
| キーボード | `Cmd/Ctrl + Shift + L` |
| サイドバー | 🎓アイコンをクリック |
| コマンドパレット | `Learning Mode`を実行 |

### 初回セットアップ

1. 学習モードを起動
2. ProfileSetupViewでプロファイルを入力
   - エンジニア経験年数
   - 主な役割
   - 技術習熟度
   - 学習ゴール
   - 学習スタイル
3. 「保存」をクリック

### カリキュラム生成

1. プロファイル設定後、CurriculumViewが表示
2. 「カリキュラムを生成」ボタンをクリック
3. 生成進捗を確認（分析→生成→完了）
4. カリキュラムが表示される

### タスク管理

1. タスクの左のチェックボックスでステータス変更
2. タスクをクリックで詳細パネルを開く
3. 詳細パネルでステータス変更・ファイルジャンプ・解説依頼

### 統計確認

- カリキュラム画面上部で進捗統計を確認
- 章別進捗バーで各章の状況を把握

### エクスポート

1. エクスポートボタンをクリック
2. 形式（Markdown/JSON）を選択
3. 統計を含めるかどうかを選択
4. ファイルがダウンロードされる

### コード解説

1. タスク詳細パネルで「💡 コードを解説」をクリック
2. Clineチャットが開始
3. AIが関連ファイルを読んで解説を生成

---

## gRPC API一覧

### LearningService

| RPC | タイプ | 説明 |
|-----|--------|------|
| saveProfile | Unary | プロファイル保存 |
| getProfile | Unary | プロファイル取得 |
| initEnvironment | Unary | 環境初期化 |
| detectTechStack | Unary | 技術スタック検出 |
| getCurriculum | Unary | カリキュラム取得 |
| generateCurriculum | Server Streaming | カリキュラム生成 |
| updateTaskProgress | Unary | タスク進捗更新 |
| getLearningStatistics | Unary | 統計取得 |
| exportCurriculum | Unary | エクスポート |

---

## データモデル

### UserProfile

```typescript
{
  experienceLevel: "less_than_1_year" | "1_to_3_years" | "3_to_5_years" | "more_than_5_years"
  primaryRole: "frontend" | "backend" | "fullstack" | "mobile" | "devops" | "other"
  techStackProficiency: { [technology: string]: "no_experience" | "basic" | "practical" | "expert" }
  learningGoal: "overview" | "feature_development" | "architecture" | "code_review"
  learningStyle: "theory" | "hands_on" | "sample_code"
}
```

### Curriculum

```typescript
{
  id: string
  title: string
  description: string
  projectSummary: string
  chapters: [{
    id: string
    title: string
    description: string
    order: number
    tasks: [{
      id: string
      title: string
      description: string
      status: "not_started" | "in_progress" | "completed" | "skipped"
      targetFiles: string[]
      estimatedTime: string
      prerequisites: string[]
    }]
  }]
  createdAt: string
  updatedAt: string
}
```

### LearningStatistics

```typescript
{
  curriculumId: string
  totalTasks: number
  completedTasks: number
  inProgressTasks: number
  skippedTasks: number
  notStartedTasks: number
  estimatedTotalMinutes: number
  actualTimeSpentMinutes: number
  completionPercentage: number
  lastActivityTime: string
  streakDays: number
  learningDates: string[]
  taskStats: [{
    taskId: string
    startedAt: string
    completedAt: string
    timeSpentMinutes: number
  }]
  chapterProgress: [{
    chapterId: string
    chapterTitle: string
    totalTasks: number
    completedTasks: number
    progressPercentage: number
  }]
}
```

---

## 関連ドキュメント

- [Phase 2: カリキュラム生成](./Phase2.md)
- [Phase 3: タスク実行支援](./Phase3.md)
- [実装計画](../.claude/plans/swirling-snacking-kitten.md)

---

## 更新履歴

| 日付 | 更新内容 |
|------|----------|
| 2025-12-23 | Phase 3完了、全機能実装完了 |
